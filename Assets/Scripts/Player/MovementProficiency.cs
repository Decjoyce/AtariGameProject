using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementProficiency : MonoBehaviour
{
    public float proficiencyIntensity;
    // Start is called before the first frame update
    void Start()
    {
        proficiencyIntensity = 1.2f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
